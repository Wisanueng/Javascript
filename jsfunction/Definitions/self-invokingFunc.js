// Self-Invoking Function คือฟังก์ชันที่เรียกใช้งานตัวเองทันทีโดย ไม่ต้องถูกเรียก (call) จากที่อื่น

// 🔹 หลักการของ Self-Invoking Function
// - ต้องใช้ Function Expression เท่านั้น ✅
// - ใช้วงเล็บ ( ... ) ครอบฟังก์ชันทั้งหมด เพื่อบอกว่าเป็น Expression
// - ตามด้วย () เพื่อเรียกใช้งานตัวเองทันที

// พอมาหัวข้อนี้ ทำให้เข้าใจว่า function expression ไม่ได้หมายถึงการประกาศฟังชันแบบเก็บไว้ในตัวแปรซะทีเดียว
// แต่เป็นการประกาศ Function แบบไม่มีชื่อนั่นเอง   (ความเข้าใจส่วนตัวซึ่งก็ไม่ผิดซะทีเดียว แต่ก็ไม่ขอยืนยันว่าถูกต้องและครอบคลุมไปซะทีเดียวเช่นกัน) 
// ซึ่งหากไม่มีชื่อก็จะเรียกใช้ไม่ได้(ถ้าไม่ invoking) เลยต้องเก็บไว้ในตัวแปร แล้วเรียกใช้ผ่านตัวแปรเอา

// Example

        (function () {
            console.log("Hello, I run myself!");
        })(); // ✅ ฟังก์ชันนี้จะทำงานทันที

console.log("=================================================================================================");


// โจทย์

// เขียนฟังก์ชันที่เรียกตัวเองเพื่อแสดงข้อความ "This is a self-invoking function"

        (function () {
            console.log("This is a self-invoking function");
        })();

console.log("=================================================================================================");

// สร้างฟังก์ชันที่ใช้การเรียกตัวเอง (self-invoking function) เพื่อคำนวณผลบวกของตัวเลข 1 ถึง 5

        (function () {
            let sum = 0;
            for(let i = 1; i <=5 ;i++){
                sum += i;
            }
            console.log(`ผลบวกของเลข 1 - 5 คือ ${sum} `);
            
        })();

console.log("=================================================================================================");

// เขียนฟังก์ชันที่เรียกตัวเองเพื่อแสดงข้อความว่า "I am executed automatically"

        (function () {
            console.log("I am executed automatically");
        })();

console.log("=================================================================================================");

// สร้างฟังก์ชัน self-invoking ที่รับสองตัวเลขและแสดงผลลัพธ์ของการบวกกัน
        (function (num1 , num2) {
            console.log(num1 + num2);
        })(3,2);

console.log("=================================================================================================");

// เขียนฟังก์ชันที่เรียกตัวเองเพื่อแสดงผลลัพธ์ของการหาผลคูณของตัวเลข 6 และ 7
        (function () {
            console.log(6 * 7);
        })();

console.log("=================================================================================================");

        