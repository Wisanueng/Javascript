// Rest Parameters (...)
// โจทย์
console.log("=================================================================================================");

// สร้างฟังก์ชัน sumAll ที่ใช้ ... เพื่อรับค่าหลายตัวและคืนค่าผลรวมของมัน

        const sumAll = (...numbers) => numbers.reduce((total, num) => total + num, 0);

        // function Sumall = (...numbers){
        //     return numbers.reduce((total ,num) {
        //         total + num;
        //     } 0;)
        // }

        function sumAll(...numbers) {
            return numbers.reduce(function (total, num) {
              return total + num;
            }, 0);
          }
          

console.log("=================================================================================================");
console.log("=================================================================================================");
console.log("=================================================================================================");
console.log("=================================================================================================");
console.log("=================================================================================================");
console.log("=================================================================================================");
